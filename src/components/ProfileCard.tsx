type CardProps = {
  id: number;
  name: string;
  designation: string;
  rate: number;
  picture: string;
};

const Card = ({ id, name, designation, rate, picture }: CardProps) => {
  return(

 <div className="min-h-screen bg-gradient-to-r from-pink-400 via-red-300 to-orange-300" >

 </div>

   );
};

export default Card;
